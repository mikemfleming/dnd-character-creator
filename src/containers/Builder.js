import React, { Component } from 'react';

export default class Race extends Component {
    constructor (props) {
      super(props);
      this.state = {};
    }

    render () {
      return <h1>Frig</h1>
    }
};

// navbar element
// character element begins at "select class"
// steps